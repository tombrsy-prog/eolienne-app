{
  "name": "eolienne-app",
  "version": "0.1.0",
  "description": "Generateur generique de donnees + detection d'anomalies (squelette)",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "demo": "tsc -p tsconfig.node.json && node dist-node/demo.js"
  },
  "devDependencies": {
    "vite": "^8.0.16"
  }
}
